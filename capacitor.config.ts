import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.koala.app',
  appName: 'ActualTutorial',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
